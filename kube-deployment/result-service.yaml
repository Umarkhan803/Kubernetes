apiVersion: v1
kind: Service
metadata:
  name: result-services
  labels:
    app-name: vote-services
spec:
  type: NodePort 
  ports:
    - name: result-services
      port: 8081  
      targetPort: 80
      nodePort: 31001
  selector:
    app: result