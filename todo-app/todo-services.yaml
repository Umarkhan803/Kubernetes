apiVersion: v1
kind: Service
metadata:
  name: todo-services
  labels:
    app: todo-services
spec:
   type: NodePort
   ports:
    - port: 3000
      targetPort: 3000
      nodePort: 31000
    - port: 3000
      targetPort: 3000
      nodePort: 31000
   selector:
     app: todo-app