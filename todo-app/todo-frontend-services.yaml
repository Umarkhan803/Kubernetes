apiVersion: v1
kind: Service
metadata:
  name: todo-frontend-services
  labels:
    app: todo-frontend-services
spec:
   type: NodePort
   ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 31000
   selector:
    app: todo-frontend-pod