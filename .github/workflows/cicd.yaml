name: cd

on:
    push:
     branches: [main]

jobs:
    deploy:
        name: deploy in vps
        runs-on: ubuntu-latest
        environment: production
        steps:
            - name: Deploy over SSH
              uses: appleboy/ssh-action@v1.0.3
              with:
                host: ubuntu
                username: umar
                password: Umar@123
                script: |
                 
                  git pull