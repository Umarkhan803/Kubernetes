name: cd

on:
    push:
     branches: [main]

jobs:
    deploy:
        name: deploy in vps
        runs-on: self-hosted
        environment: production
        steps:
            - name: Pull latest code
              run: |
                cd /home/umar/kube-project/Kubernetes/kubernetes-demo
                git pull
            # - name: Deploy over SSH
            #   uses: appleboy/ssh-action@v1.0.3
            #   with:
            #     host: ubuntu
            #     username: umarecho "4ef2f25285f0ae4477f1fe1e346db76d2f3ebf03824e2ddd1973a2819bf6c8cf  actions-runner-linux-x64-2.335.1.tar.gz" | shasum -a 256 -c
            #     password: Umar@123
            #     script: |
                 
            #       git pull